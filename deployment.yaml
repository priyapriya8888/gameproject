apiVersion: apps/v1
kind: Deployment
metadata:
  name: snake-game
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snake-game
  template:
    metadata:
      labels:
        app: snake-game
    spec:
      containers:
      - name: snake-game
        image: vishnupriya68/snakegame:latest   # ✅ Corrected image name
        ports:
        - containerPort: 5000
